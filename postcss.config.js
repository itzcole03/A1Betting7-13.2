// Root PostCSS config intentionally minimal to avoid resolving plugins from repo root.
// Frontend uses its own PostCSS config in `frontend/postcss.config.js` and Vite `css.postcss`.
export default {
  plugins: {},
};
