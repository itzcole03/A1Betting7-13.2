// Importing the module should register chart components without throwing
import '../../utils/chart';

describe('utils/chart registration', () => {
  it('module imports cleanly', () => {
    // If import succeeded, test will reach here
    expect(true).toBe(true);
  });
});
