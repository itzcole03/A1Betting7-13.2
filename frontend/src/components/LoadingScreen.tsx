import React from 'react';

const LoadingScreen: React.FC = () => (
  <div style={{ padding: 24, color: '#2563eb', background: '#f0f9ff', borderRadius: 8 }}>
    <h2>Loading onboarding...</h2>
  </div>
);

export default LoadingScreen;
