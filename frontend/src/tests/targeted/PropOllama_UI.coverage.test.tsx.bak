// Wrapper to allow Jest to discover and execute the existing PropOllama UI test
// (original test lives under src/components/user-friendly/__tests__ and may be ignored
// by testPathIgnorePatterns). Importing the test file here causes Jest to execute it
// while placing the wrapper in a discoverable path so coverage can be collected per-test.
import '../../components/user-friendly/__tests__/PropOllama.test.tsx';
