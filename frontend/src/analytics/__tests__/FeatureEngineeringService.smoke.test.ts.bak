import * as FeatureEngineeringService from '../FeatureEngineeringService';

describe('FeatureEngineeringService (smoke)', () => {
  it('module loads and exports something', () => {
    expect(FeatureEngineeringService).toBeDefined();
  });
});
