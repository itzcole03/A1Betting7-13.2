import * as PrizePicksAPI from '../../api/PrizePicksAPI';

describe('PrizePicksAPI (smoke)', () => {
  it('module loads and exports something', () => {
    expect(PrizePicksAPI).toBeDefined();
  });
});
