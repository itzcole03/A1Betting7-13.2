import { PrizePicksAdapter } from '../../adapters/prizepicks';

describe('PrizePicksAdapter (smoke)', () => {
  it('fetchData returns an object (stub)', async () => {
    const adapter = new PrizePicksAdapter();
    const res = await adapter.fetchData();
    expect(typeof res).toBe('object');
    expect(res).toEqual({});
  });
});
